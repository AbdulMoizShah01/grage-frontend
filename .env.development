# Development - uses Vite's proxy to hit the deployed backend when available
VITE_API_URL=/api
# Try the remote spendings API first, but fall back to local storage if the backend route isn't available.
VITE_SPENDINGS_API_MODE=auto
