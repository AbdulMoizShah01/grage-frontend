# Development - uses Vite's proxy
VITE_API_URL=/api
# Spendings API isn't available on the hosted backend yet.
# Force the UI to stay in local mode so we don't keep hitting the missing endpoint.
VITE_SPENDINGS_API_MODE=local
