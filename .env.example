VITE_API_URL=/api
# Spendings API modes: "auto" (try backend then fallback), "local", "remote"
# Tip: use "local" while developing until the backend exposes /api/spendings.
VITE_SPENDINGS_API_MODE=auto
