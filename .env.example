VITE_API_URL=/api
# Spendings API modes: "auto" (try backend then fallback), "local", "remote"
# Use "remote" in production so spendings are persisted in the deployed backend.
VITE_SPENDINGS_API_MODE=remote
