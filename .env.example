# API Configuration
VITE_API_URL=/api

# For local development, the Vite dev server proxies /api requests to http://localhost:8080
# In production, you can set the full API URL if needed:
# VITE_API_URL=https://api.example.com/api